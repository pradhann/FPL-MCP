# Mark the `mix_server` directory as a Python package. Without
# this file, relative imports within the package (e.g. from
# ``.tools`` or ``..utils``) will fail when the code is executed
# outside of an MCP context.